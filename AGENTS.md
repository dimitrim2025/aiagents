# AGENTS.md

## Cursor Cloud specific instructions

This repository (`aiagents`) is currently empty — it contains only a `README.md`. There is no application code, no dependency files, no build system, and no tests.

- **No dependencies to install.** There is no `package.json`, `requirements.txt`, `go.mod`, `Makefile`, or similar.
- **No services to run.** There are no backend, frontend, or database services.
- **No lint, test, or build commands.** No tooling is configured yet.

When application code is added in the future, update this file with:
1. How to install dependencies (e.g., `npm install`, `pip install -r requirements.txt`).
2. How to run the dev server(s).
3. How to run lint and tests.
4. Any non-obvious setup caveats discovered during development.
